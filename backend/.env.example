# Neon PostgreSQL (Primary - use this for cloud)
DATABASE_URL=postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/health_journal?sslmode=require

# Local PostgreSQL (Fallback - for local development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=health_journal
DB_USER=postgres
DB_PASSWORD=your_password

# JWT Secret
JWT_SECRET=your_jwt_secret_key_here

# AI Microservice Configuration
AI_SERVICE_URL=http://localhost:8000

# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000